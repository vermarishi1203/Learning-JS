
// trying the isArray() method 

// checking if certain variable/values are arrays or not

const numArr = [1, 2, 3, 4, 5, 6];

const strOfNum = "123456";

const arrOfStr = ["A", "B", "C"];

const someNum = 259;

const someObj = {name: "Rishi", age: 58};

console.log( Array.isArray( numArr ));

console.log( Array.isArray( strOfNum ));

console.log( Array.isArray( arrOfStr ));

console.log( Array.isArray( someNum ));

console.log( Array.isArray( someObj ));

